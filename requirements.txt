# Python 3.11 — ATRS standalone (subset of the repo-root unified requirements.txt)
# Setup:
#   conda create -n atrs python=3.11 && conda activate atrs && pip install -r requirements.txt
# torch pulls the CUDA 12.1 wheel; for CPU-only, drop --extra-index-url and the +cu121 suffix.

--extra-index-url https://download.pytorch.org/whl/cu121

# Deep learning framework
torch==2.3.1+cu121

# Aspect term extraction — PyABSA. transformers 4.44.2 works via a 1-line tokenizer-pickle
# compat shim in src/aspect_extraction.py; en_core_web_sm is auto-loaded by PyABSA.
transformers==4.44.2
tokenizers==0.19.1
pyabsa==2.4.2
seqeval>=1.2.0
termcolor>=2.0.0
update-checker==0.18.0             # 1.0.0 breaks metric_visualizer (PyABSA dep): check() signature changed
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Word embeddings (Word2Vec)
gensim==4.3.3

# Data / ML
numpy==1.26.4
pandas>=2.0.0
pyarrow>=12.0.0
# scikit-learn 1.8+ deadlocks with torch+cu121 native libs on read_json — pin to 1.5.x.
scikit-learn==1.5.2

# NLP / text
nltk>=3.9.0
beautifulsoup4>=4.12.0
contractions>=0.1.73

# Utilities
PyYAML>=6.0.0
tqdm>=4.66.0
